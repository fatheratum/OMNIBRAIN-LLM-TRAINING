model:
  hidden_size: 512
  num_layers: 6
  num_attention_heads: 8
  vocab_size: 32000
  max_position_embeddings: 2048

training:
  learning_rate: 5e-5
  batch_size: 4
  gradient_accumulation_steps: 4
  warmup_steps: 100
  max_steps: 5000

data:
  max_seq_length: 512
